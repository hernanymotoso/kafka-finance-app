name=elasticsearch-sink
connector.class=io.confluent.connect.elasticsearch.ElaelasticsearchSinkConnector
tasks.max=1
topics=mysql-server.fin.transactions
connection.url=http://es01:9200
type.name=_doc
value.converter=io.confluent.connect.json.JsonConverter
value.converter.schemas.enable=true
transforms=extractValue
transforms.extractValue.type=org.apache.kafka.connect.transforms.ExtractField$Value
transforms.extractValue.field=after
